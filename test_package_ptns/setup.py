import setuptools

setuptools.setup(
    name = "test_package_ptns",
    version = "0.0.1",
    author = "don",
    author_email = "d.vandenbergh@uva.nl",
    python_requires = ">=3.6",
    packages = setuptools.find_packages()
)